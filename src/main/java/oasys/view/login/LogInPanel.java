package oasys.view;

import javax.swing.*;
import java.awt.*;

public class LogIn extends JPanel {
    public LogIn() {
        setBackground(Color.BLUE);
        setPreferredSize(new Dimension(600,400));
    }
}
